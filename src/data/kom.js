/**
 * Données de fallback pour le KOM du mois (si API indisponible)
 * @type {import('../types').KomMonthly}
 */
export const currentKom = null;

/**
 * Historique des KOM précédents
 * @type {import('../types').KomMonthly[]}
 */
export const komHistory = [];
